version: '3.1'

services:

  tgbot1:
    container_name: in_support_max
    build: .
    volumes:
      - .:/src
    command: python app.py
    restart: always
    network_mode: "docker_mysql_default"

    env_file:
      - ".env"
